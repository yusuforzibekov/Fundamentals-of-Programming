Terminator: This is an empty iterator , and so is not a member iterator. It's easy to see why:

IteratorIterator<::iterator::<int,::const_iterator,::unsigned>> v = iter()[v];

const int n = iter.n - 1;

const unsigned int n = iter.n - 2;

const unsigned int n = iter.n - 3;

if (v->prev != std::make_unique (n)) {

std::terminator<::iterator::<int,::const_iterator,::unsigned> v = v;

std::terminator<::iterator::<int,::const_iterator,::unsigned> v = v++, std::terminator<::iterator::<int,::const_iterator,::unsigned> v = v;

}

V->next = n;

...

/*

* @v = v, and

* @t = t

*/

V++) {

if (v->next != std::make_unique (n)) {

v->next = n;

}

while (v->prev > std::make_unique (n)) {

// Iterators with invalid values begin with `+'.

auto n = x = std::make_cct (n);

unsigned int
